import React from 'react';
import { Box, Typography } from '@mui/material';

const ForwardingRulesPage: React.FC = () => (
  <Box>
    <Typography variant="h4" gutterBottom>Regras de Reencaminhamento</Typography>
    <Typography variant="body1">Página em desenvolvimento...</Typography>
  </Box>
);

export default ForwardingRulesPage;
